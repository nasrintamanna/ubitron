
raw_acc/
proc_gyro/
acc_32Hz/
gyro_32Hz/
merged_acc_gyro/
labeled_acc_gyro/
segmented_4s/
balanced_folds/
rf_features/
original_labels/
feature_labels/

# Jupyter / Python housekeeping
.ipynb_checkpoints/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store

# Secrets (HF_TOKEN etc.) - never commit
.env

# Trained models for upgraded_pipeline/ - 371 MB and 398 MB, over GitHub's
# 100 MB file limit. Regenerate them (about 3 minutes each) with:
#   python3 upgraded_pipeline/train_final_model.py            # with time of day
#   python3 upgraded_pipeline/train_final_model.py --no-time  # fallback
# Their small metadata files (models/*.json) are still committed.
upgraded_pipeline/models/*.joblib

# Integrity-check fingerprints (135 MB each): working files listing every file
# in the project, used to verify nothing outside a work folder was modified.
_snapshot_*.txt

# Intermediate results - regenerated by the scripts
Try_increase_accuracy/cache/
Final_result/cache/

